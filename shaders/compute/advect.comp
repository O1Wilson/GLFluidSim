#version 460 core
layout(local_size_x = 16, local_size_y = 16) in;
layout(r32f, binding = 0) uniform image2D dst;

void main() {
	ivec2 gid = ivec2(gl_GlobalInvocationID.xy);
	imageStore(dst, gid, vec4(0.2, 0.7, 0.4, 0.9));
}